# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.52
pydantic-settings==2.7.0
python-multipart==0.0.20
python-dotenv==1.0.1
bcrypt==4.2.1
python-jose[cryptography]==3.3.0
httpx==0.28.1

# Database drivers — install the one matching DATABASE_URL.
# MySQL (local XAMPP default):
aiomysql==0.2.0
# PostgreSQL (Supabase / Neon / RDS). Optional for local MySQL.
# Skipped on 3.14+ until wheels catch up (compiling from source fails on Windows).
asyncpg==0.30.0; python_version < "3.14"

# Data connectors (CSV uses the stdlib; Excel needs openpyxl)
openpyxl==3.1.5
